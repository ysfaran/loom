{
  "name": "core",
  "projectType": "library",
  "sourceRoot": "packages/core/src",
  "root": "packages/core",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @loom/core build"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @loom/core typecheck"
      }
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm --filter @loom/core clean"
      }
    }
  }
}
