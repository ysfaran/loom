# CLI Reference

These are the current MVP commands.

## Commands

- `loom list [path]`
- `loom validate [path]`
- `loom build [path] --out <path>`
- `loom dev [path] --port <port>`

## Notes

- Paths are recursive.
- MDX syntax errors fail validation and build.

Back to [Home](/).
