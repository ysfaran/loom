# Loom Template Docs

Welcome to the local Loom template.

Use this folder to test `loom list`, `loom validate`, `loom build`, and `loom dev`.

## Quick Start

- Open [Getting Started](/getting-started)
- Open [CLI Reference](/reference/cli)

## Example Code

```ts
export function hello(name: string): string {
  return `Hello ${name}`;
}
```
